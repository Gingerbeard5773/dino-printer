[versions]
mod-version = "1.2.0"

# Fabric (https://fabricmc.net/develop)
minecraft = "1.21.5"
yarn-mappings = "1.21.5+build.1"
fabric-loader = "0.16.10"

# Loom (https://github.com/FabricMC/fabric-loom)
loom = "1.14-SNAPSHOT"

# Meteor (https://github.com/MeteorDevelopment/meteor-client/)
meteor = "1.21.5-SNAPSHOT"

# Mods
litematica = "0.22.5"
malilib = "0.24.3"

[libraries]
# Fabric base
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
yarn = { module = "net.fabricmc:yarn", version.ref = "yarn-mappings" }
fabric-loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric-loader" }

# Meteor client
meteor-client = { module = "meteordevelopment:meteor-client", version.ref = "meteor" }

# Mods
litematica = { module = "fi.dy.masa.litematica:litematica-fabric-1.21.5", version.ref = "litematica" }
malilib = { module = "fi.dy.masa.malilib:malilib-fabric-1.21.5", version.ref = "malilib" }

[plugins]
fabric-loom = { id = "fabric-loom", version.ref = "loom" }
